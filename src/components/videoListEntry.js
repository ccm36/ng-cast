angular.module('video-player')

.component('videoListEntry', {
  // bindings
  bindings: {
  },
  controller: function() {
    // console.log(this);
  },
  // controller

  // template
  templateUrl: 'src/templates/videoListEntry.html'
});
